{
  "name": "lrjas",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c green,cyan \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:actividades": "npm run dev --prefix frontend-actividades",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install --prefix frontend-actividades",
    "deploy:prod": "bash deploy.sh"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
